Live Sketch (Python & OpenCV)

A real-time pencil sketch effect created using Python and OpenCV.
The program captures webcam frames, converts them to grayscale, applies Gaussian blur, detects edges, and blends them to produce a smooth sketch output.

Features:

Real-time webcam processing

Grayscale + Gaussian blur

Edge detection

Pencil sketch effect generation
